const SET_MANAGEMENT = "SET_MANAGEMENT";
const PICK_MANAGEMENT = "PICK_MANAGEMENT";
const PICK_REMOVE_MANAGEMENT = "PICK_REMOVE_MANAGEMENT";

const SET_SIZES = "SET_SIZES"; // 사이즈 저장
const SET_JOIN_FIRST = "SET_JOIN_FIRST"; // 회원가입 1-1
const SET_JOIN_SECOND = "SET_JOIN_SECOND"; // 회원가입 1-2

const POST_PHONE = "POST_PHONE";
const PATCH_PHONE = "PATCH_PHONE";

const SET_JOIN = 'SET_JOIN';
const POST_JOIN = 'POST_JOIN';

export default {
  PICK_MANAGEMENT,
  PICK_REMOVE_MANAGEMENT,
  SET_SIZES,
  SET_JOIN_FIRST,
  SET_JOIN_SECOND,
  SET_MANAGEMENT,

  POST_PHONE,
  PATCH_PHONE,
  SET_JOIN,
  POST_JOIN
};
